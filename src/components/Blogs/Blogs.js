import React from 'react';
import './Blogs.css'

const Blogs = () => {
    return (
        <div className='blog'>
        <div >
        <h3>Context API</h3>
                <p>context API is a React structure.
                    it solve the prop-drilling from all of your application.
                    context API is a new feature in react version.
                    that allows one to share state across the entire 
                    app lightly and with ease.
                </p>
        </div>
        <div>
        <h3>Semantic Tag</h3>
                <p>Semantic tag explain describely not only the 
                    browser but also to developer.  In programming, 
                    Semantics refers to the meaning of a piece of code. 
                    there are two types of it ome is semintic and is non semintic.
                    </p>
        </div>
        </div>
    );
};

export default Blogs;